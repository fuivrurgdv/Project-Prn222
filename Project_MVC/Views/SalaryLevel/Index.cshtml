@model IEnumerable<Project_MVC.Models.SalaryLevel.SalaryLevelDTO>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-controller="SalaryLevel" asp-action="GetSalaryLevel">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                SalaryLevelId
            </th>
            <th>
                LevelName
            </th>
            <th>
                BasicSalary
            </th>
            <th>
                Description
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @item.SalaryLevelId
            </td>
            <td>
                @item.LevelName
            </td>
            <td>
                @item.BasicSalary
            </td>
            <td>
                @item.Description
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
